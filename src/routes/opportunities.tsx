import { createFileRoute } from "@tanstack/react-router";
import { BriefcaseBusiness, Filter, Sparkles } from "lucide-react";
import { opportunities } from "@/lib/kala-data";
import { AppShell, Button, SectionHeading } from "@/components/kala/ui";
import { OpportunityList } from "@/components/kala/cards";

export const Route = createFileRoute("/opportunities")({ head: () => ({ meta: [{ title: "Opportunities For You — sih 2026" }, { name: "description", content: "Explore AI-matched market opportunities for your craft." }, { property: "og:title", content: "Opportunities For You — sih 2026" }, { property: "og:description", content: "Find market briefs matched to your craft, price, capacity, and location." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Opportunities });
function Opportunities() { return <AppShell title="Opportunities for you" eyebrow="AI market matching"><div className="mx-auto max-w-6xl"><section className="mt-7 flex flex-col gap-5 rounded-2xl border border-indigo/15 bg-indigo-soft p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div className="flex items-start gap-3"><div className="grid size-11 shrink-0 place-items-center rounded-xl bg-indigo text-primary-foreground"><Sparkles className="size-5" /></div><div><p className="font-display text-xl font-bold text-ink">Opportunities For You</p><p className="mt-1 max-w-2xl text-sm leading-relaxed text-muted-foreground">Not a product list. These are real-looking briefs where your craft, price, capacity, and region could fit.</p></div></div><div className="flex items-center gap-2 text-sm font-semibold text-indigo"><BriefcaseBusiness className="size-4" />{opportunities.length} open matches</div></section><div className="mt-8"><SectionHeading eyebrow="Ranked by fit" title="Your next market could be here" action={<Button variant="quiet"><Filter className="size-4" />Filter</Button>} /><OpportunityList /></div></div></AppShell>; }