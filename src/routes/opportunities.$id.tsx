import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, ArrowRight, Check, ChevronRight, MapPin, MessageCircle, Package, Sparkles, UsersRound } from "lucide-react";
import { opportunities } from "@/lib/kala-data";
import { AppShell, Button, DemoBanner, MatchBar, Score, SectionHeading } from "@/components/kala/ui";
import { OpportunityMeta } from "@/components/kala/cards";

const fallbackOpportunity: Opportunity = opportunities[0] ?? { id: "fallback", title: "Opportunity", buyer: "—", score: 0, quantity: "—", budget: "—", location: "—", deadline: "—", requirement: "—", category: "—", materials: "—", reasons: [] };

export const Route = createFileRoute("/opportunities/$id")({ head: () => ({ meta: [{ title: "Opportunity Details — sih 2026" }, { name: "description", content: "See why sih 2026 recommended this market opportunity for your craft." }, { property: "og:title", content: "Opportunity Details — sih 2026" }, { property: "og:description", content: "Understand the match before you express interest." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: OpportunityDetails });
function OpportunityDetails() { const { id } = Route.useParams(); const navigate = useNavigate(); const opportunity = opportunities.find((item) => item.id === id) ?? fallbackOpportunity; return <AppShell title="Opportunity details" eyebrow="A closer look"><div className="mx-auto max-w-5xl"><Link to="/opportunities" className="mb-6 inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Back to opportunities</Link><DemoBanner /><div className="mt-7 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-terracotta">{opportunity.category}</p><h1 className="mt-2 font-display text-3xl font-bold tracking-tight text-ink">{opportunity.title}</h1><p className="mt-2 flex items-center gap-1.5 text-sm text-muted-foreground"><UsersRound className="size-4" />{opportunity.buyer}</p></div><Score value={opportunity.score} /></div><div className="mt-6"><MatchBar value={opportunity.score} /></div><p className="mt-6 text-base leading-relaxed text-foreground/80">{opportunity.requirement}</p><div className="mt-7"><OpportunityMeta opportunity={opportunity} /></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-border p-4"><p className="text-xs text-muted-foreground">Product category</p><p className="mt-1 font-semibold">{opportunity.category}</p></div><div className="rounded-xl border border-border p-4"><p className="text-xs text-muted-foreground">Preferred materials</p><p className="mt-1 font-semibold">{opportunity.materials}</p></div></div></div><aside className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-7"><SectionHeading eyebrow="Why AI recommended this" title="A strong fit" description="sih 2026 compares the details that matter for a successful order." /><div className="space-y-4">{[["Product match", 95], ["Price compatibility", 92], ["Capacity match", 88], ["Location suitability", 90]].map(([label, value]) => <div key={label as string}><div className="mb-1.5 flex justify-between text-sm"><span>{label}</span><strong className="text-indigo">{value}%</strong></div><MatchBar value={value as number} /></div>)}</div><div className="mt-7 rounded-xl bg-saffron/15 p-4 text-sm leading-relaxed text-saffron-foreground"><div className="flex items-center gap-2 font-semibold"><Sparkles className="size-4" />What happens next?</div><p className="mt-2">Your interest is sent to the buyer. They can review your catalogue and start a conversation.</p></div><Button className="mt-6 min-h-12 w-full" onClick={() => { window.alert("Interest sent in demo mode. See Buyer Enquiries for the next step."); navigate({ to: "/enquiries" }); }}>Express interest <ArrowRight className="size-4" /></Button><Link to="/enquiries" className="mt-3 flex items-center justify-center gap-1 text-sm font-semibold text-terracotta">View buyer enquiries <ChevronRight className="size-4" /></Link></aside></div></div></AppShell>; }