import { createFileRoute } from "@tanstack/react-router";
import { Check, MessageCircle, MoreHorizontal, X } from "lucide-react";
import { enquiries } from "@/lib/kala-data";
import { AppShell, Button, SectionHeading, StatusBadge } from "@/components/kala/ui";

export const Route = createFileRoute("/enquiries")({ head: () => ({ meta: [{ title: "Buyer Enquiries — sih 2026" }, { name: "description", content: "Manage conversations and interest from buyers who want your craft." }, { property: "og:title", content: "Buyer Enquiries — sih 2026" }, { property: "og:description", content: "See who is interested in your products and choose the next step." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Enquiries });
function Enquiries() { return <AppShell title="Buyer enquiries" eyebrow="Your conversations"><div className="mx-auto max-w-5xl"><SectionHeading eyebrow="Stay connected" title="Buyer enquiries" description="Simple next steps for every buyer conversation." action={<span className="rounded-full bg-saffron/20 px-3 py-1.5 font-mono text-[10px] uppercase text-saffron-foreground">3 conversations</span>} /><div className="space-y-3">{enquiries.map((enquiry) => <article key={enquiry.id} className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><StatusBadge status={enquiry.status} /><p className="font-mono text-[10px] uppercase tracking-[.15em] text-muted-foreground">Buyer enquiry</p></div><h2 className="mt-3 font-display text-xl font-bold text-ink">{enquiry.buyer}</h2><p className="mt-1 text-sm text-muted-foreground">Interested in <strong className="text-foreground">{enquiry.interest}</strong></p></div><button aria-label="More enquiry actions" title="More enquiry actions" className="self-end rounded-xl p-2 text-muted-foreground hover:bg-muted sm:self-start"><MoreHorizontal className="size-5" /></button></div><div className="mt-5 grid grid-cols-2 gap-4 rounded-xl bg-muted/60 p-4 text-sm sm:grid-cols-4"><div><p className="text-xs text-muted-foreground">Quantity</p><p className="mt-1 font-semibold">{enquiry.quantity}</p></div><div><p className="text-xs text-muted-foreground">Budget</p><p className="mt-1 font-semibold">{enquiry.budget}</p></div><div><p className="text-xs text-muted-foreground">Required by</p><p className="mt-1 font-semibold">{enquiry.deadline}</p></div><div><p className="text-xs text-muted-foreground">Delivery</p><p className="mt-1 font-semibold">{enquiry.delivery}</p></div></div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => window.alert("Enquiry accepted in demo mode.")}><Check className="size-4" />Accept</Button><Button variant="quiet" onClick={() => window.alert("Opening a demo conversation.")}><MessageCircle className="size-4" />Discuss</Button><Button variant="quiet" onClick={() => window.alert("Enquiry declined in demo mode.")}><X className="size-4" />Decline</Button></div></article>)}</div></div></AppShell>; }