import { createFileRoute, Link } from "@tanstack/react-router";
import { Eye, MoreHorizontal, Pencil, Plus, Share2 } from "lucide-react";
import { products } from "@/lib/kala-data";
import { AppShell, Button, SectionHeading, StatusBadge } from "@/components/kala/ui";

export const Route = createFileRoute("/products")({ head: () => ({ meta: [{ title: "My Products — KalaSetu AI" }, { name: "description", content: "Manage your published craft catalogues and drafts." }, { property: "og:title", content: "My Products — KalaSetu AI" }, { property: "og:description", content: "Keep your artisan catalogue clear, current, and ready for buyers." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Products });
function Products() { return <AppShell title="My products" eyebrow="Your catalogue"><div className="mx-auto max-w-6xl"><SectionHeading eyebrow="Your catalogue" title="My products" description="Three products are helping buyers discover your craft." action={<Link to="/add-product"><Button><Plus className="size-4" />Add product</Button></Link>} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{products.map((product) => <article key={product.id} className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><img src={product.image} alt={product.imageAlt} width={912} height={912} loading="lazy" className="aspect-[4/3] w-full object-cover" /><div className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-lg font-bold leading-tight text-ink">{product.title}</h2><p className="mt-1 text-xs text-muted-foreground">{product.category}</p></div><button aria-label={`More actions for ${product.title}`} title="More actions" className="rounded-lg p-1 text-muted-foreground hover:bg-muted"><MoreHorizontal className="size-5" /></button></div><p className="mt-4 font-display text-xl font-bold text-indigo">{product.price}</p><div className="mt-3"><StatusBadge status={product.status} /></div><div className="mt-5 grid grid-cols-3 gap-2"><button className="flex min-h-10 items-center justify-center gap-1 rounded-xl border border-border text-xs font-semibold hover:bg-muted"><Pencil className="size-3.5" />Edit</button><button className="flex min-h-10 items-center justify-center gap-1 rounded-xl border border-border text-xs font-semibold hover:bg-muted"><Eye className="size-3.5" />View</button><button className="flex min-h-10 items-center justify-center gap-1 rounded-xl border border-border text-xs font-semibold hover:bg-muted"><Share2 className="size-3.5" />Share</button></div></div></article>)}</div></div></AppShell>; }