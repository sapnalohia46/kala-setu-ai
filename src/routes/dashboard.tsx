import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Eye, MessageCircle, PackageOpen, Plus, Sparkles } from "lucide-react";
import { opportunities, products, demoArtisan, enquiries } from "@/lib/kala-data";
import { AppShell, Button, DemoBanner, SectionHeading } from "@/components/kala/ui";
import { MiniOpportunity } from "@/components/kala/cards";

export const Route = createFileRoute("/dashboard")({ head: () => ({ meta: [{ title: "Artisan Dashboard — sih 2026" }, { name: "description", content: "A simple artisan workspace for products, enquiries, and AI-matched market opportunities." }, { property: "og:title", content: "Artisan Dashboard — sih 2026" }, { property: "og:description", content: "See your craft catalogue and next market opportunities." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Dashboard });

const fallbackEnquiry: Enquiry = { id: "none", status: "New", buyer: "—", interest: "—", quantity: "—", budget: "—", deadline: "—", delivery: "—" };

function Dashboard() { const latest = enquiries[0] ?? fallbackEnquiry; return <AppShell title="Home" eyebrow="Good morning, Meena"><div className="mx-auto max-w-6xl"><DemoBanner /><section className="mt-7 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-terracotta">Your craft workspace</p><h1 className="mt-2 font-display text-3xl font-bold tracking-tight text-ink sm:text-4xl">Namaste, Meena <span className="text-terracotta">👋</span></h1><p className="mt-2 text-sm text-muted-foreground">Your next opportunity may already be looking for you.</p></div><Link to="/add-product"><Button className="min-h-12"><Plus className="size-4" />Add new product</Button></Link></section><div className="mt-8 grid grid-cols-2 gap-3 lg:grid-cols-4"><Summary icon={PackageOpen} label="Active products" value="3" tone="indigo" /><Summary icon={Sparkles} label="Market matches" value="4" tone="saffron" /><Summary icon={MessageCircle} label="Buyer enquiries" value="3" tone="terracotta" /><Summary icon={Eye} label="Profile views" value="18" tone="moss" /></div><section className="mt-10"><SectionHeading eyebrow="Next best step" title="Recommended opportunities" description="AI-ranked from your craft, capacity, price, and location." action={<Link to="/opportunities" className="text-sm font-semibold text-terracotta">See all <ArrowRight className="ml-1 inline size-4" /></Link>} /><div className="grid gap-3 lg:grid-cols-3">{opportunities.slice(0, 3).map((opportunity) => <MiniOpportunity key={opportunity.id} opportunity={opportunity} />)}</div></section><section className="mt-10 grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-2xl border border-border bg-indigo p-5 text-primary-foreground sm:p-6"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-primary-foreground/70">AI catalogue review</p><h2 className="mt-2 font-display text-2xl font-bold">Your blue pottery vase is almost market-ready.</h2><p className="mt-2 max-w-lg text-sm leading-relaxed text-primary-foreground/75">Review the AI draft, add your voice, then publish when it feels right.</p></div><Sparkles className="size-7 text-saffron" /></div><Link to="/catalogue" className="mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl bg-saffron px-4 py-2.5 text-sm font-bold text-saffron-foreground hover:bg-saffron/90">Review catalogue <ArrowRight className="size-4" /></Link></div><div className="rounded-2xl border border-border bg-card p-5 sm:p-6"><SectionHeading eyebrow="Inbox" title="Latest enquiry" /><div className="rounded-xl bg-muted/60 p-4"><div className="flex items-center justify-between"><p className="font-display font-bold text-ink">{latest.buyer}</p><span className="rounded-full bg-saffron/20 px-2.5 py-1 font-mono text-[10px] uppercase text-saffron-foreground">New</span></div><p className="mt-2 text-sm text-muted-foreground">Interested in {latest.interest}</p><p className="mt-3 text-sm font-semibold">{latest.quantity} · {latest.budget}</p><Link to="/enquiries" className="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-terracotta">Open enquiries <ArrowRight className="size-4" /></Link></div></div></section><p className="mt-10 text-center font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground/70">{demoArtisan.craft} · {demoArtisan.capacity}</p></div></AppShell>; }
function Summary({ icon: Icon, label, value, tone }: { icon: typeof PackageOpen; label: string; value: string; tone: "indigo" | "saffron" | "terracotta" | "moss" }) { const styles = { indigo: "bg-indigo-soft text-indigo", saffron: "bg-saffron/20 text-saffron-foreground", terracotta: "bg-terracotta-soft text-terracotta", moss: "bg-moss-soft text-moss" }; return <div className="rounded-2xl border border-border bg-card p-4 shadow-sm"><div className={`grid size-9 place-items-center rounded-xl ${styles[tone]}`}><Icon className="size-4" /></div><p className="mt-4 font-display text-2xl font-bold text-ink">{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></div>; }