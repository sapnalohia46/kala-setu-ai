import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowRight, Check, Sparkles } from "lucide-react";
import { useEffect } from "react";
import { AppShell, Button } from "@/components/kala/ui";

export const Route = createFileRoute("/publish-success")({ head: () => ({ meta: [{ title: "Product Market-Ready — KalaSetu AI" }, { name: "description", content: "Your product catalogue is ready and AI is searching for market opportunities." }, { property: "og:title", content: "Product Market-Ready — KalaSetu AI" }, { property: "og:description", content: "KalaSetu is finding the right market for your craft." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: PublishSuccess });
function PublishSuccess() { const navigate = useNavigate(); useEffect(() => { const timer = window.setTimeout(() => navigate({ to: "/opportunities" }), 3600); return () => window.clearTimeout(timer); }, [navigate]); return <AppShell title="Product published" eyebrow="Step 3 · Market matching"><div className="mx-auto flex min-h-[65vh] max-w-2xl items-center justify-center"><div className="w-full rounded-3xl border border-border bg-card p-7 text-center shadow-sm sm:p-12"><div className="mx-auto grid size-20 place-items-center rounded-full bg-moss-soft text-moss"><Check className="size-10" /></div><p className="mt-7 font-mono text-[10px] uppercase tracking-[.2em] text-terracotta">🎉 Your product is market-ready!</p><h1 className="mt-3 font-display text-3xl font-bold tracking-tight text-ink sm:text-4xl">The right buyers can now find your craft.</h1><p className="mx-auto mt-4 max-w-md text-sm leading-relaxed text-muted-foreground">AI is now searching for suitable market opportunities based on your product, price, capacity, and location.</p><div className="mx-auto mt-8 flex max-w-sm items-center gap-2 rounded-xl bg-indigo-soft px-4 py-3 text-left"><span className="grid size-8 shrink-0 place-items-center rounded-full bg-indigo text-primary-foreground"><Sparkles className="size-4 animate-pulse-soft" /></span><span className="text-sm text-indigo">Finding your best market matches<span className="animate-pulse-soft">...</span></span></div><Link to="/opportunities" className="mt-7 inline-block"><Button>See opportunities <ArrowRight className="size-4" /></Button></Link></div></div></AppShell>; }